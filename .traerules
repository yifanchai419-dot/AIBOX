# 项目全局 AI Agent 规则与行为准则 (.traerules)

## 1. 项目背景与目标
* **项目名称**：AI知识日报与课程生成智能体
* **核心目标**：开发一个智能体，每日自动采集 AI 领域新动态（模型/产品/论文/行业/技巧），进行去重、质量打分与自动分类，并将其重构编排为结构化的 1 课时（约 60 分钟）培训教案（Markdown 格式）。
* **开发原则**：最节约、最经济、代码模块化、具备极强的容错性（支持 Mock 模式与异常处理）。

## 2. 技术栈与架构约束
* **语言与版本**：Python 3.10+
* **前端 UI**：Streamlit（轻量级 Web 交互界面）
* **信息采集**：feedparser（解析 RSS）、requests + beautifulsoup4（网页抓取与 HTML 文本清洗）
* **存储与向量**：SQLite（本地轻量数据库）、ChromaDB（本地向量库，用于查重与知识库比对）
* **大模型接口**：使用 OpenAI SDK 标准接口，兼容适配 DeepSeek V3/R1 等高性价比模型。

## 3. 代码编写与目录规范
* 所有核心逻辑必须按模块进行清晰划分：
  - `src/fetcher.py`: 信息采集模块
  - `src/processor.py`: 过滤去重、质量打分与分类模块
  - `src/generator.py`: 1课时教案与大纲生成模块
  - `src/vector_db.py`: 本地向量库与知识比对模块
  - `app.py`: Streamlit Web 前端主入口
* **代码容错要求**：
  - 所有网络请求与 API 调用必须包含 `try-except` 捕获机制，单个信源失败或超时不得影响整体流程。
  - 每个 Python 模块文件底部必须包含 `if __name__ == '__main__':` 本地独立测试入口。
* **注释规范**：重点函数必须有清晰的 Docstring（包含参数说明与返回值类型）。

## 4. 输出教案结构标准 (Markdown)
当生成课程教案时，必须严格遵守以下结构：
1. 课程标题与基本信息（目标、适合对象、总时长 60 min）
2. 模块一：热点引入 (10 min)
3. 模块二：核心新知讲解 (30 min)
4. 模块三：互动研讨与思考题 (15 min)
5. 模块四：总结与延伸阅读 (5 min)

## 5. Agent 交互准则
* 在修改或生成代码前，请确保符合上述 Python 架构约束。
* 生成代码后，自动在终端或提示中明确告知用户如何运行测试。
* 如果检测到用户缺少 API Key，应优先使用 Mock/样例数据保证业务流程的完整运行。